# Catatan: dtoverlay=gpio-shutdown TIDAK dipakai lagi.
# Kontrol restart 5-detik-tahan ditangani oleh button_control.py + button-control.service.
# Boot otomatis dari kondisi mati tetap berjalan bawaan hardware GPIO3, tanpa overlay tambahan.

# HDMI stabilization (opsional, kalau ada masalah kedip layar terkait HDMI)
hdmi_force_hotplug=1
hdmi_group=1
hdmi_mode=16
config_hdmi_boost=4
